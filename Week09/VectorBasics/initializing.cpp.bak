#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main() {
    vector<int> counts;
    vector<string> words;

    vector<int> numbers; //empty
    vector<double> measurements(3); //three doubles initialized to 0.0
    vector<string> fruits(4, "kiwi"); 

    vector<int> counts2 = {5, 6, 3, 2};
    vector<string> words2 = {"pear", "apple", "banana"};
}
