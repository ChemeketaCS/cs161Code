#include <iostream>

using namespace std;

int main() {
  char firstInitial = 'c';

  char nextLetter = static_cast<char>(firstInitial + 1);
  cout << nextLetter << endl;

  char prevLetter = static_cast<char>(firstInitial - 1);
  cout << prevLetter << endl;
}
