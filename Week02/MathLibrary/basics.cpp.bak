#include <cmath> // need to include for sqrt()
#include <iostream>

using namespace std;

int main() {
  double x = 17;
  double root = sqrt(x); // call sqrt and give it x as input
  cout << "The square root of " << x << " is " << root << endl;
}
