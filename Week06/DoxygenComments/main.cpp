#include <iomanip>
#include <iostream>

using namespace std;

/**
 * @brief Caculates the sum of all the numbers from start to end
 * @param start Value to start from - must be lower than end
 * @param end Last value to count (inclusive)
 * @return Sum of all values
 */
int sum(int start, int end) {
  // code goes here
}

/**
 * @brief Finds the maximal temperature over a range of days
 * @param start Day of the year representing the starting point
 *              of the range.
 *              Examples: Jan 1st = 0, Feb 1st = 31
 * @param range How many days in addition to start day to search
 * @return Max temp in degrees C
 */
double getMaxTemperature(int start, int range);

/**
 * @brief Prints value with $ in front rounded to two places
 * @param amount Value in dollars.
 *               e.g. 1.02 for one dollar and two cents
 */
void printAsMoney(double amount) {
  // code goes here
}

/**
 * @brief Get the high temperature (in Fahrenheight) for
 * the specified day of the year
 * @param day Day of the year. Jan 1st = 1, Feb 1st = 32...
 * @return degrees F
 */
double getForecastTemperature(int day) {
  // code goes here
}

int main() {
  
}
