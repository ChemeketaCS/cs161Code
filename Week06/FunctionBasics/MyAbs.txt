#include <iostream>
#include <iomanip>

using namespace std;


//returns absolute value of a number
double myabs(double number) {
    if (number < 0)
        number = -number;

    return number;
}

////Failed version - does not return a value on all paths
//double myabs(double number) {
//    if (number < 0) {
//        return -number;
//        //does not return a value!
//    }
//}

int main()
{
    cout << myabs(12) << endl;
    cout << myabs(-6.5) << endl;
    cout << myabs(0) << endl;
}

