#include <cmath>
#include <iostream>
using namespace std;

// Global constant is fine
const double PI = 3.14159;

double getCircleArea(double radius) {
  return PI * pow(radius, 2);
}

double getCircleCircumference(double radius) {
  return PI * 2 * radius;
}

int main() {
}
